import { pageSeo } from "@/presentation/seo/metadata";

export const metadata = pageSeo.supplierDashboard;

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}
